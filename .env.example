# ===== 公众号文章抓取（微信公众平台登录态）=====
# 自己实现抓取，不依赖任何第三方 RSS 服务。需要已登录的 mp.weixin.qq.com 会话。

# 1) mp.weixin.qq.com 登录后的完整 Cookie（必须含 data_ticket / slave_sid 等）。
#    获取：浏览器登录 https://mp.weixin.qq.com -> F12 -> Network -> 任意请求 ->
#          Request Headers -> Cookie 整段复制。必填。
MP_COOKIE=

# 2) mp 后台地址栏 URL 里的 token=xxxxxx（数字串）。建议带上，部分账号校验更稳。选填。
MP_TOKEN=

# 3) 要抓取的公众号列表，多个用 | 分隔（可选，会与 accounts.txt 合并去重）。
#    每行/每项格式支持两种：
#       名称             （仅名称，会尝试用搜狗解析 biz，可能失败）
#       名称,biz         （推荐：直接给出 __biz，跳过解析）
# 例如：二鸟说,MzAxxxx|韭菜投资学,MzBxxxx
WX_ACCOUNTS=

# 仓库内维护的账号文件，每行一个（默认 accounts.txt）。
# 可用 sync_booklist.py 从微信读书书单同步「名称」，但建议补上 biz 以保证可抓取。
# 格式：账号名  或  账号名,biz（# 开头为注释）
# ACCOUNTS_FILE=accounts.txt

# （可选）每个号最多抓取的篇数，默认 20
# MAX_ARTICLES=20

# （可选）RSS 输出目录，默认 feeds
# OUT_DIR=feeds

# ===== 微信读书书单（仅用于发现公众号，可选）=====
# 书单页为公开 SSR 无需登录。供 sync_booklist.py 使用，不设置则用脚本内置默认书单。
# WEREAD_BOOKLIST_URLS=https://weread.qq.com/misc/booklist/19335103_81YvqKoRv

# 微信读书 Cookie——本项目仅用 sync_booklist.py 从「书单」发现公众号，
# 书单页公开无需登录；因此本变量目前不是必需的，保留以备扩展。
# WEREAD_COOKIE=

# ===== 火山方舟（结构化分析，可选但推荐）=====
# 可插拔分析器（如二鸟说手抄报）用 Ark 做 LLM 结构化抽取。
# 仅在运行 src/analyze.py 时需要；不配置则分析器跳过（仅保留订阅源）。
# ARK_API_KEY=ark-xxxx
# ARK_MODEL=doubao-seed-2-1-pro-260628
# ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

